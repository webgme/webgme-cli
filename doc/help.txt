usage: webgme [action] [item] [options]
available actions:    <% _.forEach(actions, function(action) { %>
    <%= action.name %>:<% _.forEach(action.items, function(item) { %>
        <%= item %><% }); %><% }); %>
