#!/usr/bin/env node

'use strict';
var utils = require('../src/utils'),
    nop = require('nop'),
    RouterManager = require('../src/RouterManager');

var manager = new RouterManager();

manager.ls({}, nop);
