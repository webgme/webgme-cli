#!/usr/bin/env node

'use strict';
var utils = require('../src/utils'),
    nop = require('nop'),
    AddonManager = require('../src/AddonManager');

var manager = new AddonManager();

manager.ls({}, nop);
