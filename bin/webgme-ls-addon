#!/usr/bin/env node

'use strict';
var utils = require('../lib/utils'),
    nop = require('nop'),
    AddonManager = require('../lib/AddonManager');

var manager = new AddonManager();

manager.ls({}, nop);
