#!/usr/bin/env node

'use strict';
var utils = require('../src/utils'),
    nop = require('nop'),
    LayoutManager = require('../src/LayoutManager');

var manager = new LayoutManager();

manager.ls({}, nop);
