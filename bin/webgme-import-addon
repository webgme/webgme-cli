#!/usr/bin/env node
/*jshint node: true*/

'use strict';
var Command = require('commander').Command,
    program = new Command(),
    utils = require('../lib/utils'),
    nop = require('nop'),
    AddonManager = require('../lib/AddonManager');

var manager = new AddonManager();

program.arguments('<name> <project>')
    .option('-d,--dev', 'Import component for dev use only')
    .option('-n,--name [pkg-name]', 'Set installed package name (in package.json)')
    .action(function(name, project) {
        program.pkgName = program.name;
        program.name = name;
        program.project = project;
        manager.import(program, nop);
    })
    .parse(process.argv);
