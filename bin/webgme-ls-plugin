#!/usr/bin/env node

'use strict';
var utils = require('../src/utils'),
    nop = require('nop'),
    PluginManager = require('../src/PluginManager');

var manager = new PluginManager();

manager.ls({}, nop);
