#!/usr/bin/env node

'use strict';
var utils = require('../lib/utils'),
    nop = require('nop'),
    PluginManager = require('../lib/PluginManager');

var manager = new PluginManager();

manager.ls({}, nop);
